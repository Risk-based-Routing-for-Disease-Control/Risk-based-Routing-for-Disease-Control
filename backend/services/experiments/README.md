# Experimental dispatch references

This directory contains evaluation and demo-oriented routing experiments.

These files are not part of the production API path. They are kept to show the
algorithm exploration behind the dispatch demo, including clustering and ALNS
variants that may be useful for later migration work.

Before using any code here in production, validate the assumptions, constraints,
runtime behavior, and input data quality against the actual field operation
policy.
